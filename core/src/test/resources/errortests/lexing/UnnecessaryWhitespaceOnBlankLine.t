// ignore
// The error message worked the last time I tested it manually but the
// test cannot be automated currently since the error no longer appears
// if you put a comment after the whitespaces. This could be solved
// by stripping the test files of the marker comments before running the tests.
// tests.

Def Test() =
	// comment
		
     


	println(5)