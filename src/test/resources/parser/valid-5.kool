object Program {
  def main(): Unit = {
    var a: Int;
    var b: Int;
    var c: Int;
    a = 4 / 3 * 6;
    b = ((4 / 3) * 6);
    c = 6 * 4 / 3;
    d = ((6 * 4) / 3;
    if (a == b == c == d) {
      println("true");
    } else {
      println("false");
    }
    var d: Int[];
    println(d.length);
    d = new Int[10]
    println(d.length);
    println("abc");
    println(10);
    var obj: Object;
    obj = new Object()
  }
}

class A {
  var a: Int;
  var b: Bool;
  var c: String;
  var d: Object;

  def Method1(): Int = {
    if(0 < 1 || 1 - 1 < 1 || true && true) if (false) b = false;
    if(!false) a = 1; else if(true) a = 2 else if(true) a = 3;
    while(a < 10) while(a < 10) while(a < 10) { b = 10} 
    d = d.Method2();
    d = d.Method2(true);
    d = d.Method2(true, 1);
    d = d.Method2(true, 1, "lol");
    return 0;
  }

  def Method2(): String = {
    return c;
  }  
}

class B extends A {
  var d: Object;

  def Method2(): Object = {
    d = new Int[10];
    d[0] = 1;
    d[1] = 2;
    d[2] = 3;
    println(d.length);
    return d;
  }

}