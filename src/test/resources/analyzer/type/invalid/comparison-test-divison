32

object Main {
  def main() : Unit = {
    println(new A().foo());
  }
}

class A { 
  def foo(): Int = {
    println(0 / new A());
    println(0 / "S");
    println(0 / true);
    println(0 / new Int[0]);
    println(new A() / 0);
    println(new A() / "S");
    println(new A() / true);
    println(new A() / new Int[0]);
    println("S" / 0);
    println("S" / new A());
    println("S" / true);
    println("S" / new Int[0]);
    println(true / 0);
    println(true / new A());
    println(true / "S");
    println(true / new Int[0]);
    println(new Int[0] / 0);
    println(new Int[0] / new A());
    println(new Int[0] / "S");
    println(new Int[0] / true);
    return 0; 
  }
}
