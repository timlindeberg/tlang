object Program4 {
  def/**/main () : Unit = {
    if(new MMain().run()) println("");
  }
}

class MMain {
  def run(): Bool = {
    var a1: A;
    var a2: A;
    var b1: B;
    var b2: B;
    var compA: Comparator[A];
    var compB: Comparator[B];
    var discard: Int;

    compA = new Comparator[A]();
    compB = new Comparator[B]();
    a1 = new A();
    a2 = new A();
    b1 = new B();
    b2 = new B();
    println(compA.compare(a1, a2));
    println(compB.compare(b1, b2));
    return false;
  }
}

class Comparator[T] {
  def compare(t1: T, t2: T): Bool = {
    return t1.equals(t2);
  }
}

class A {
  def equals(other: A): Bool  = {
    return this == other;
  }
}

class B {
  def equals(other: B): Bool = {
    return true;
  }
}
