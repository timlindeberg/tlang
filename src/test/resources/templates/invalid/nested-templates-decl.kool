object Program {
  def main () : Unit = {
    if(new Main().run()) println("");
  }
}

class Main {
	def run(): Bool = {
		var a: A<Int, String>;
		a = new A<Int, String>();
		println(a.run(5));
		return false;
	}
}

class A<T<X>, U> {
	
	def run(t: T): T = {
		return t;
	}
}