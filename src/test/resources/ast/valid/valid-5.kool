object Program4 {
  def/**/main () : Unit = {
    println(new Test().init());
  }
}

class Test {
  def init() : Test = {
    var expr1 : Boolean;
    var expr2 : Boolean;
    var a : Int;
    if(expr1) { if(expr2) { a=1; } else { a=2; } }
    if(expr1) if(expr2) a=1; else a=2;
    if(expr1) { if(expr2) { a=1; } } else { a=2; }
    return this;
  }
}